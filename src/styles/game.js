import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  
  container: {
    flex: 1,
    backgroundColor: '#000',
    // borderWidth: 3,
    // borderColor: '#FFF',
  }
});

export default styles;